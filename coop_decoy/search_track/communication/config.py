"""
通信模块配置常量

所有数字阈值、报文长度、发送频率均集中在此文件，
禁止在其他模块中硬编码。
"""

# ==========================================================================
# 消息约束
# ==========================================================================

PAYLOAD_MAX_BYTES = 50           # 单条消息最大字节数 (UTF-8)，引擎硬限制
SEND_RATE_HZ      = 4            # 每秒最大发送次数，引擎硬限制
COMM_RANGE_M      = 1000         # 通信距离上限 (米)
INBOX_MAX_SIZE    = 32           # 收件箱最大消息数

# ==========================================================================
# 坐标编码基准（压缩编解码用）
# ==========================================================================

BASE_LAT     = 27.0
BASE_LON     = 125.0
COORD_SCALE  = 100_000          # 0.00001° ≈ 1.1 m

# ==========================================================================
# 坐标系精度
# ==========================================================================

COORD_DECIMALS = 5              # 浮点坐标保留的小数位数

# ==========================================================================
# 飞行约束
# ==========================================================================

SPEED_MIN_MPS = 15              # 最小飞行速度 m/s
SPEED_MAX_MPS = 40              # 最大飞行速度 m/s
FOV_MIN_DEG   = 5               # 最小视场角 (度)
FOV_MAX_DEG   = 120             # 最大视场角 (度)

# ==========================================================================
# 仿真参数
# ==========================================================================

SIM_DURATION_S  = 600           # 仿真总时长 (秒)
DECIDE_RATE_HZ  = 10            # decide() 调用频率 (Hz)
TICK_PERIOD_S   = 1.0 / DECIDE_RATE_HZ  # 每次 decide 间隔 (秒)
